# Finance Tracker - Client

This is a simple React + Vite frontend for the Finance Tracker app.

Setup:

```powershell
cd client
npm install
npm run dev
```

Default dev server: http://localhost:5173

The app expects the API at `http://localhost:4000/api` (see server/README.md).
